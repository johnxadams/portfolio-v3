WeatherPage
line64
  // getTime(); 
  // I do Math.floor insdie timezoneOffsetHours because some timeZones devided by 3600, comes with a dezima.
  // e.g. Adelaide timezone 34200 / 360 = 9,5

  // console.log("time: ", time);
  // console.log("timeHours as a number:  ", timeHours);
  // console.log("timezoneOffsetHours: ", timezoneOffsetHours);
  // console.log("foreignTimezoneHour: ", foreignTimezoneHour);
  // console.log("worldClock: ", worldClock);

  // const bgWeather = data.weather?.[0].main.toLowerCase();

  // console.log(bgWeather);
  // console.log(data.weather?.[0].main.toLowerCase());



Setting up a border with a function

export const setupBorder = ({width, type, color}) => {
  return `${width}px ${type} ${color}`
}


Pokemon Page
#1
/**
 * Fetch data from API using asyncronous Function.
 * Fetching the API needs to be outside of the react-function-component. #rfc
 * Otherwise the API-fetching will initialize everytime the web-page renders.
 */

#2
 // hp: {hpNr: pokeData.stats[0].base_stat, hpName: pokeData.stats[0].stat.name},
        // for some reason this doesnt work as an object!!!

#3
    {/*
    * I saved two classNames inside the pokeData Object.
    * 'pokemon-name-container' and 'img-container'  
    * If I do styling, using classNames from the pokeData,
    * The stlying only appears onSubmit='handlePokemonSearch'
    *
    */}